<body class="login">
<div class="container" align="center" style="margin-top: 200px;margin-bottom: 180px;">
<h2>개인정보 수정하기 : <%= resource_name.to_s.humanize %><br/><br/></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>

<table width="450px;" border="0">
  <tr border="0" border-color="black">
    <td>
    <div class="field">
      <%= f.label :이메일 %><br />
      <%= f.email_field :email, class: "form-control", autofocus: true %>
      <br/>
    </div>
    </td>
  </tr>
  <tr>
    <td>
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
      <% end %>
    </td>
  </tr>
  <tr>
    <td>
      <div class="field">
        <%= f.label :현재_비밀번호 %> <i>(정보 저장을 위해서는 현재 비밀번호를 입력해주세요.)</i><br/>
        <%= f.password_field :current_password, class: "form-control", autocomplete: "off" %>
      </div>
    </td>
  </tr>
  <tr>
    <td>
      <hr/>
      <div class="field">
        <%= f.label :변경할_비밀번호 %> <i><br/>(만약 비밀번호 변경을 원치 않을 경우 그대로 냅두세요.)</i><br/>
        <%= f.password_field :password, class: "form-control", autocomplete: "off" %>
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> 글자 이상)</em>
        <br/><br/>
        <% end %>
      </div>
    </td>
  </tr>
  <tr>
    <td>
      <div class="field">
      <%= f.label :변경할_비밀번호_재확인 %><br />
      <%= f.password_field :password_confirmation, class: "form-control", autocomplete: "off" %>
      </div>
    </td>
  </tr>
  <tr>
    <td>
      <br/>
      <div class="actions" align="center">
        <%= f.submit "정보 저장", class: "btn btn-primary" %>
        <br/><br/>
      </div>
      <% end %>
    </td>
  </tr>
  <tr>
    <td>
      <hr/>
      <br/>
      <h3><i class="fa fa-sign-out" aria-hidden="true"></i> 계정 삭제하기</h3>
      <p>계정을 삭제하시겠어요..?... <%= button_to "계정 삭제하기...", registration_path(resource_name), class: "btn btn-danger", data: { confirm: "정말요?ㅠㅠ" }, method: :delete %></p>
    </td>
  </tr>
</table>
<br/><br/>
<%= link_to "돌아가기", :back, class: "btn btn-success" %>
</div>
</body>